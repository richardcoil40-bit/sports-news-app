/**
 * Web has no native splash to hand off from, so there's nothing to overlay.
 * The file exists to satisfy the platform-specific import in `_layout.tsx`.
 */
export function AnimatedSplashOverlay() {
  return null;
}
