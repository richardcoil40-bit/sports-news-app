{
  "name": "nofrills-verdicts",
  "version": "0.1.0",
  "private": true,
  "description": "Headline classification service for NoFrills. Deployed separately from the app; see README.md.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260815.1",
    "typescript": "~5.9.2",
    "wrangler": "^4.0.0"
  }
}
